services:
  - type: web
    name: terraform-mcp-server
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:mcp.app --host 0.0.0.0 --port $PORT
